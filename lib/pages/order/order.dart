import 'package:flutter/material.dart';

class Order extends StatelessWidget {
   Order({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Container(height: 50, width: 50, color:Color(0xFFD6F4FF),),
    );
  }
}